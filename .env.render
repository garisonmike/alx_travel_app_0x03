# ═══════════════════════════════════════════════════════════
# 🔐 RENDER DEPLOYMENT - ENVIRONMENT VARIABLES
# ═══════════════════════════════════════════════════════════
#
# Copy these variables to your Render service's "Environment" tab
# Replace placeholder values with your actual credentials
#
# ═══════════════════════════════════════════════════════════

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔑 REQUIRED VARIABLES
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Django Secret Key (REQUIRED)
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-change-this-to-random-50-char-string

# Debug Mode (REQUIRED - MUST be False in production)
DEBUG=False

# Allowed Hosts (REQUIRED - Replace with your Render URL)
# Format: domain1,domain2,domain3 (comma-separated, no spaces)
ALLOWED_HOSTS=alx-travel-app.onrender.com,localhost

# Database URL (REQUIRED - Auto-filled by Render PostgreSQL)
# Render automatically provides this when you link a database
# Format: postgresql://user:password@host:5432/dbname
DATABASE_URL=${postgresql://auto-filled-by-render}

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🐰 CELERY / RABBITMQ CONFIGURATION (REQUIRED)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# RabbitMQ Broker URL (REQUIRED - From CloudAMQP)
# Get free RabbitMQ at: https://www.cloudamqp.com/
# Copy the AMQP URL from your CloudAMQP dashboard
CELERY_BROKER_URL=amqps://username:password@server.cloudamqp.com/vhost

# Celery Result Backend (REQUIRED)
CELERY_RESULT_BACKEND=rpc://

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📧 EMAIL CONFIGURATION (REQUIRED for notifications)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Email Backend
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# SMTP Configuration (Gmail example)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# Email Credentials
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here

# Default From Email
DEFAULT_FROM_EMAIL=your-email@gmail.com

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 💳 OPTIONAL: PAYMENT GATEWAY
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Chapa Payment Gateway (Optional)
CHAPA_SECRET_KEY=your-chapa-secret-key-here

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🌐 OPTIONAL: CORS CONFIGURATION
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# CORS Allowed Origins (if you have a frontend)
# Format: https://domain1.com,https://domain2.com
# CORS_ALLOWED_ORIGINS=https://your-frontend.com

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔒 SECURITY (Automatically enabled when DEBUG=False)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# These are automatically configured in settings.py when DEBUG=False
# No need to set them manually:
# - SECURE_SSL_REDIRECT=True
# - SESSION_COOKIE_SECURE=True
# - CSRF_COOKIE_SECURE=True
# - SECURE_BROWSER_XSS_FILTER=True
# - SECURE_CONTENT_TYPE_NOSNIFF=True
# - X_FRAME_OPTIONS=DENY
# - SECURE_HSTS_SECONDS=31536000
# - SECURE_HSTS_INCLUDE_SUBDOMAINS=True
# - SECURE_HSTS_PRELOAD=True

# ═══════════════════════════════════════════════════════════
# 📋 RENDER DEPLOYMENT CHECKLIST
# ═══════════════════════════════════════════════════════════
#
# Before deploying, ensure you have:
#
# ✅ Generated SECRET_KEY (use Django command above)
# ✅ Set DEBUG=False
# ✅ Updated ALLOWED_HOSTS with your Render URL
# ✅ Created CloudAMQP account and copied AMQP URL
# ✅ Configured email credentials (Gmail App Password)
# ✅ Created PostgreSQL database in Render
# ✅ Linked database to web service (auto-fills DATABASE_URL)
#
# ═══════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════
# 🔗 USEFUL LINKS
# ═══════════════════════════════════════════════════════════
#
# Render Dashboard: https://dashboard.render.com/
# CloudAMQP: https://www.cloudamqp.com/
# Gmail App Passwords: https://myaccount.google.com/apppasswords
# Django Secret Key Generator: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# ═══════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════
# 📖 FOR MORE DETAILS
# ═══════════════════════════════════════════════════════════
#
# See: RENDER_DEPLOYMENT.md (comprehensive deployment guide)
#
# ═══════════════════════════════════════════════════════════
